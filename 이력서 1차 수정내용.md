# 이력서 1차 수정내용

> `resume.html` 개선 작업 기록. AI(Claude Code)와 함께 진행한 과정과 판단 근거를 정리.

---

## 1. A4 페이지 분할

### 문제
기존 `resume.html`은 모든 섹션(헤더~PR)이 `<main>` 하나에 들어 있어서, 인쇄/PDF 변환 시 A4(297mm) 한 장을 넘는 분량이 애매한 위치에서 잘렸다.

### 조치
- 실제 렌더링 높이를 직접 측정(Puppeteer로 각 섹션의 `getBoundingClientRect().height`를 mm 단위로 계산)해서, 어느 지점에서 나눠야 두 페이지 모두 297mm 안에 들어오는지 확인.
- 처음엔 "프로젝트 중간(Kanto 다음)"에서 나눴더니 1페이지가 394mm까지 넘쳐서 3페이지로 밀렸음 → 실측 기반으로 재조정.
- 최종적으로 **1페이지: 헤더·소개·교육·기술 스택 / 2페이지: 프로젝트 전체·PR**로 분리 (1페이지 288mm, 2페이지 290mm — 둘 다 297mm 이내).
- `print:break-before-page` CSS로 인쇄 시에도 정확히 이 지점에서 페이지가 나뉘도록 처리.
- 우측 하단에 `1 / 2`, `2 / 2` 페이지 번호 표시 추가.

**교훈**: 감으로 분량을 나누지 않고, 실제 렌더링 크기를 측정해서 분할 지점을 정했다. "대충 맞겠지"가 아니라 숫자로 확인하는 방식.

---

## 2. Kanto 프로젝트 기여 내용 보강 — 보고서 기반 검증

### 배경
팀 프로젝트(Kanto)에서 실제로 어떤 역할을 했는지, 본인이 정리해둔 기여 보고서(`kanto.md`)를 근거로 이력서 문구를 하나씩 대조했다. "그럴듯해 보이는 단어"가 아니라 **문서에 실제 근거가 있는 표현만** 남기는 방식으로 접근.

### 검증 과정에서 걸러낸 표현들

| 처음 쓰려던 표현 | 검증 결과 | 조치 |
|---|---|---|
| **QA** | 보고서에서 "QA"는 `release(QA)` 브랜치 이름으로만 등장 — 본인이 테스트/품질보증 업무를 맡았다는 근거 없음 | 삭제 |
| **테크리드** | 팀이 수평적 기여도 기반 구조라 공식 타이틀 근거 없음. 다만 "코드 리뷰·핫픽스 대응 업무를 팀원별로 할당·추적하는 문서를 직접 작성"한 구체적 사실은 있음 | "팀 협업 프로세스 주도"로 대체 (근거 있는 표현으로 치환) |
| **인프라 설계** | Vercel/Supabase/Upstash는 전부 관리형 서비스(BaaS/PaaS) — 서버·네트워크를 직접 구축한 게 아니라 "연동해서 쓴 것". 실제 인프라(VPC, 오토스케일링, IaC 등) 경험과는 다름 | "서버리스 아키텍처 설계"로 범위를 좁혀서 정확히 표현 |

**원칙**: 면접에서 "그거 구체적으로 어떻게 하셨어요?"라는 꼬리질문에 답할 수 있는 표현만 쓴다. 답이 궁색해질 표현은 아무리 있어 보여도 뺀다.

### 추가한 내용
보고서에 있던 다음 사례를 이력서에 반영:
> AI로 리포지토리 전체를 분석해 미완성 라우트(404) 등 문제를 선제적으로 발견해 팀에 공유, 스코프 조정 판단으로 연결

"문제를 먼저 제기하고 의사소통을 이끄는 편"이라는 막연한 자기평가 대신, 실제 에피소드(AI로 자가 진단 → 문제 발견 → 팀 공유 → 의사결정)로 증명하는 방식을 택함.

### TanStack Query 도입 사례 추가
`kanto.md`에 "어드민 콘텐츠 영역 리팩터링 — TanStack Query 도입으로 서버/UI 상태 분리, 캐시 교체 방식으로 불필요한 재요청 차단"이라는 기록이 있었음. 범위는 어드민 콘텐츠 영역 하나로 한정된 리팩터링이지만, "문제(불필요한 재요청)를 발견하고 목적에 맞는 라이브러리를 도입한 기술적 판단" 자체가 신입 포지션에 어필할 만한 소재라 판단해 별도 불릿으로 반영:
> 어드민 콘텐츠 영역의 불필요한 재요청 문제를 발견해 TanStack Query 도입 — 서버/UI 상태 분리, 캐시 교체 방식으로 GET 요청 감소

기술 스택 목록에도 `TanStack Query`를 추가. Kanto 항목의 불릿이 총 7개로 늘어나 2페이지 분량이 빠듯해질 수 있어, 실제 인쇄/PDF에서 페이지 넘침 여부는 별도 확인이 필요함.

### GET 요청 감소 수치 실측 (18회 → 3회, 약 83.3% 감소)
"GET 요청 감소"라는 정성적 표현 대신 실제 수치를 넣고 싶다는 요청에 따라 직접 실측을 진행. 처음엔 배포된 사이트를 지금 시점에서 측정해도 "이전 대비 감소량"은 알 수 없다는 문제가 있었음(비교할 예전 상태가 이미 사라짐) — 그래서 TanStack Query 도입 **전/후 코드를 각각 로컬에서 재현**해 같은 시나리오로 비교 측정.

**측정 시나리오**: 탭 5회 왕복 + 룰 등록/목록 조회 2회 왕복.

**결과**: GET 요청 18회 → 3회 (약 83.3% 감소). `resume.html`에 반영:
```
서버/UI 상태 분리, 캐시 교체 방식으로 GET 요청 감소 (18회 → 3회로 약 83.3% 감소)
```
다른 성능 수치(WebP 390MB→64MB 등)와 표기 형식을 통일하기 위해 괄호 안에 수치를 넣고 퍼센트는 굵게 처리.

**교훈**: "지금 측정하면 알 수 있겠지"가 항상 맞는 건 아님 — 비교 기준(before)이 이미 사라진 경우, 실측하려면 이전 상태를 별도로 재현해야 한다는 걸 확인한 사례.

### 누락된 기술 스택 보완 — Playwright (블로그 프로젝트)
"Kanto에도 Playwright를 썼을 것"이라는 확인 요청이 있었으나, `kanto.md`에는 Playwright 관련 기록이 전혀 없었음. 대신 `myblog.md`에서 실제 사용 근거를 발견:
> jsdom·Playwright(실제 Chromium)로 3단계 실측... 이 블로그 글 규모에서는 naive 구현으로 충분하다고 판단

전역 "기술 스택" 섹션(1페이지)에는 이미 Playwright가 있었지만, 정작 그걸 사용한 **개인 기술 블로그** 프로젝트 항목의 기술 스택 줄에는 빠져 있었음 — 확인 후 해당 줄에 추가:
```
기술 스택: JavaScript, Vite, Tailwind CSS, Supabase(DB/Storage), Docker, Playwright
```
**교훈**: "이 기술 썼던 것 같다"는 기억에만 의존하지 않고, 매번 보고서(`*.md`)에서 근거를 재확인한 뒤 반영 — 잘못된 프로젝트에 잘못 붙이는 것을 방지.

기술 스택 태그만으로는 "어디에 썼는지"가 안 드러난다는 지적을 받아, `myblog.md` 2.3절(Next.js/React 전환 적합성 검토) 내용을 근거로 별도 불릿을 추가:
> Next.js/React 전환 적합성 검토 시 Playwright(Chromium)로 블록 수별 렌더링 성능 실측 — naive 방식의 성능 한계치를 데이터로 확인, 알고리즘 이식 없이 오버엔지니어링 방지

실제 근거: jsdom·Playwright로 3단계 실측을 진행해 "약 600~700블록부터 naive 방식이 60fps 프레임 예산을 초과한다"는 걸 수치로 확인하고, 이 블로그 규모(수십~수백 줄)에는 알고리즘 이식이 오버스펙이라고 판단해 채택하지 않은 사례. 단순 기술 나열이 아니라 "실측으로 의사결정한 근거"를 불릿으로 드러낸 것.

### 성능 수치의 핵심을 잘못 짚었던 것 수정 — "8.2배(50블록)" → "문서 길이 무관 고정 시간"
기존 불릿("전체 재렌더 대비 최대 8.2배 빠름(50블록 벤치마크)")은 `myblog.md` 88번째 줄의 다른 벤치마크(고정 50블록 문서 안에서 몇 블록을 동시에 고쳤는지 비교 — 1블록 변경 8.2배, 10블록 변경 3.8배)에서 가져온 수치였음. 이 벤치마크는 "동시에 고치는 블록 수가 많아질수록 배율이 오히려 줄어드는" 얘기라, "문서가 길어질수록 npm 패키지가 유리해진다"는 실제 핵심 가치와는 다른 축이었음.

진짜 하고 싶은 얘기(문서 길이가 늘어날수록 naive 대비 격차가 커진다)는 §3.2(Playwright/Chromium 실측)에 있었음:

| 총 블록 수 | naive 평균 | block 방식 평균 |
|---|---|---|
| 100 | 2.4ms | 0.03~0.07ms (블록 수 무관 고정) |
| 700 | 20.7ms (60fps 예산 초과) | 동일 |
| 1200 | 52.4ms | 동일 |

즉 block 방식은 문서 크기와 무관하게 편집당 0.03~0.07ms로 일정한데, naive 방식은 문서가 길어질수록 계속 느려짐 — 이게 이 패키지의 실제 존재 이유. 불릿을 이 수치로 교체:
```
Before: 변경된 블록만 렌더링해 전체 재렌더 대비 최대 8.2배 빠름(50블록 벤치마크)
After:  변경된 블록만 렌더링해 문서 길이 무관 0.03~0.07ms 유지, naive 방식은 700블록부터 프레임 예산(16.6ms) 초과
```
**교훈**: 벤치마크 수치를 가져올 때, 그 수치가 "무엇을 고정하고 무엇을 변화시켜 측정한 것인지"부터 확인해야 함 — 축을 헷갈리면 숫자는 진짜인데 주장은 틀린 결론이 나올 수 있음.

### 개인 기술 블로그 — 진행 기간 표기 수정 (1차/2차 구분)
`myblog.md` 상단 기간 표기가 "2026.01.08 ~ 2026.07.11"인데, 이력서에는 "2026.01 - 2026.03"으로만 되어 있어 실제보다 짧게 보였음. 문서를 보면:
- **1차**(01~03월): Vanilla JS MPA 기반 초기 개발(v1.4.0) — 기존 이력서 내용이 다루던 부분
- **2차**(2026-07-11, "별도 세션"): README/시크릿 정리, `markdown-block-preview`의 React 전환 적합성 검토(Playwright 실측 포함), v2.0.0(Next.js/NestJS) 전환 설계 문서 작성 — 단, v2.0.0 자체는 "설계 문서만 존재, 미착수" 상태로 명시돼 있어 아직 진행 중

방금 추가한 Playwright 불릿이 바로 이 2차 세션에서 나온 작업이라, 날짜 표기를 두 구간으로 나눠 갱신:
```
Before: 2026.01 - 2026.03
After:  2026.01 - 2026.03, 2026.07~ 진행중
```

---

## 3. 맞춤법 / 띄어쓰기 / 어투 정리

문서 전체를 훑어서 아래 5곳을 수정.

| 위치 | Before | After | 사유 |
|---|---|---|---|
| 소개 문단 | 마크다운 **블록단위** 렌더링 | 마크다운 **블록 단위** 렌더링 | 문서 뒷부분(블로그 프로젝트)에서는 이미 "블록 단위"로 띄어 썼는데, 같은 표현이 앞에서만 붙어 있었음 — 표준 띄어쓰기로 통일 |
| 교육 | 과정 수료 **(**115일 / 총 920시간) | 과정 수료**(**115일 / 총 920시간) | 문서 내 다른 모든 괄호는 앞에 공백 없이 사용 — 이 줄만 예외였음 |
| Kanto 역할 | 역할 **:** ... | 역할**:** ... | "기술 스택:"은 공백 없이 쓰는데 "역할 :"만 콜론 앞에 공백이 있었음 |
| Kanto 역할 | 담당 **:** ... | 담당**:** ... | 위와 동일한 이유 |
| Kanto 문제해결 사례 | ...문제를 선제적으로 **발견·팀에 공유**, 스코프 조정 결정을 **이끎** | ...문제를 선제적으로 **발견해 팀에 공유**, 스코프 조정 **판단으로 연결** | `·`는 명사(구)끼리 대등하게 연결할 때만 쓰는데 "발견"(명사)과 "팀에 공유"(조사 포함 구)를 이어서 문법이 어긋났음. 또 "이끎"은 다른 불릿들(구현/검증/개선 등 한자어 명사형 종결)과 톤이 달라 자연스럽게 맞춤 |

---

## 4. 그 외 main 브랜치 대비 달라진 부분

이번 대화에서 직접 다루지는 않았지만, `main` 브랜치와 diff를 떠보니 함께 반영돼 있던 변경들. 브랜치 이름 자체가 `v1.0.1-nopic`인 것에서 드러나듯, 사진을 빼는 것이 이 버전의 핵심 축이었던 것으로 보임.

| 항목 | main (이전) | 현재 | 비고 |
|---|---|---|---|
| 프로필 사진 | 헤더에 `profile.jpg` 원형 사진 삽입 | 사진 완전히 제거 | 브랜치명(`nopic`)이 가리키는 핵심 변경 |
| 소개 섹션 | "장교 생활때의 체계적인 업무 습관과 꾸준함..." 등 군 경험·인성 중심의 불릿 2개 | "React·Next.js 기반으로 팀 프로젝트 1개와 개인 프로젝트 3개를 완성한..." 등 프로젝트 성과 중심의 서술형 문단 3개 | 목록형 → 문단형으로 형식 전환, 인성 어필 → 성과/숫자 중심으로 내용 전환 |
| 교육 — 세명대학교 | "학군단(ROTC) 60기 · 육군 소위 임관 (2022)" 불릿 존재 | 해당 불릿 삭제 | 장교 경력 언급을 이력서 전면에서 줄이는 방향과 일치 |
| 기타 섹션 | "KDT해커톤 - KD2TH(케디스) / 느린학습자를 위한 쉬운말 번역기 (2026.07 ~ 진행중)" 섹션 존재 | 섹션 자체 삭제 | 진행 중인/부수적인 항목 정리 |
| Frontie 프로젝트 | "Lighthouse 성능 점수 48 → 75점 개선" | "Lighthouse 성능 점수 75 → 92점 개선" | 실제 프로젝트 성능이 이후 더 개선되어 수치 갱신 |

**흐름으로 보면**: 사진·군 경력 등 "인적 사항" 성격의 정보를 줄이고, 그 자리를 프로젝트 성과·숫자로 채우는 방향으로 전체 톤이 이동했음. 이번 세션에서 진행한 A4 페이지 분할·Kanto 기여 보강·맞춤법 정리도 결국 같은 방향(성과 중심으로 밀도 있게 정리)의 연장선.

---

## 작업 방식 정리 (기록용)

1. **레이아웃은 측정으로 결정** — 감으로 페이지를 나누지 않고 실제 렌더링 높이를 재서 분할 지점을 잡음
2. **모든 문구는 근거 문서(kanto.md)와 대조** — "있어 보이는 단어"가 아니라 실제 한 일에 매핑되는 단어만 사용
3. **오버클레이밍 방지** — QA/테크리드/인프라처럼 시장에서 무거운 의미를 가진 단어는, 실제 업무 범위와 정확히 일치하는지 검증 후 사용 여부 결정
4. **표현의 일관성** — 같은 개념(블록 단위 등)이 문서 여러 곳에 등장할 때 띄어쓰기·문장부호 스타일을 통일

---

## 5. 세부 조정 (2차 라운드)

위 섹션들을 정리한 이후에도 대화가 이어지면서 실제로 더 바뀐 부분들. 특히 이 라운드에서는 "말로 그럴듯한 것"과 "숫자로 검증된 것"을 다시 한 번 걸러내는 작업이 많았다.

### Kanto — 역할/담당 구조 실험 (테이블 → 중첩 리스트)
- 처음엔 실제 `<table>`(`tr`/`td`)로 만들어봤다가, "세로로", "다시 가로로" 등 여러 레이아웃을 시도
- 최종적으로는 `<table>`을 버리고, 상위 `<li>` 안에 `border-l-0 pl-0`을 준 중첩 `<ul>`로 "역할:"/"담당:" 라벨(`flex gap-1`)을 구현 — 바깥 리스트의 점선 테두리가 중첩 시 이중으로 겹치는 문제를 `border-l-0`으로 해결
- 라벨에 넣었던 `font-bold`는 "굳이 필요 없어 보인다"는 지적에 따라 제거

### Kanto — "역할" 문구를 짧게, "담당"으로 서술 이동
- "역할"에 문장형 설명(솔선수범형 리더십, 초석 마련 등)을 넣었다가, "이력서는 보통 역할을 짧게 쓴다"는 지적에 따라 최종적으로 태그 나열형으로 축약:
  ```
  채팅 · 인증 보안 · 관리자 시스템 · 설계 문서화
  ```
- KTS·KPPS 알고리즘 설계는 "역할"이 아니라 실제 구현 목록인 "담당" 줄로 이동 — 역할은 태그, 담당은 구체적 결과물이라는 역할 분담을 명확히 함

### Kanto — Sentry는 검토 후 최종 미채택
- 실제 GitHub 커밋을 확인한 결과 Sentry 연동(에러 로그·통계 모니터링 페이지)은 본인이 2026-06-25에 직접 도입한 게 맞았음(근거 확보 완료)
- 다만 "도입했다"까지만 있고 그로 인해 측정 가능한 개선 결과가 없어서, 이미 강한 카드(WebP, TanStack Query 등)가 많은 상황에 결과 없는 항목을 끼워 넣지 않기로 판단 — 이력서에는 반영하지 않음

### Kanto — psi-check/PageSpeed 항목: 추가 → 두 차례 수정 → 최종 삭제
같은 항목을 두고 세 번 바뀌었다:
1. "측정 자동화(psi-check 스크립트)" — 실제로는 팀 컨벤션(박소유가 만든 성능감사 프레임워크)상 Google PageSpeed Insights 웹사이트로 측정했고, 스크립트 자체는 거의 활용하지 않았다는 걸 확인 → "PageSpeed Insights로 배포 후 전 페이지 재측정 — 회귀 2건을 코드 레벨로 추적해 원인 확정"으로 수정
2. "회귀 2건" 중 실제로는 1건만 회귀(ContentCard 캐러셀 eager 로딩이 되돌아간 것)고, 나머지 1건(`/go` 지도 지연로딩)은 "한 번도 제대로 동작한 적 없는 죽은 코드"라 회귀가 아니라는 지적 → "성능 저하 원인 2건"으로 재수정
3. `2차개선안.md`를 확인해보니 "담당자: 미정", "실제 수정은 이후 과제로 남김"이라고 명시돼 있어, 원인 확정까지만 했지 실제로 고쳐서 좋아졌다는 증거가 없다는 게 드러남 → 결과 없는 항목으로 판단해 **완전히 삭제**

### Kanto — Supabase 트래픽 수치 정확도 조정
- 처음엔 "(Storage Egress 390MB, 한도 88.7%)"처럼 상세 수치를 그대로 넣었으나 "너무 빡세게 읽힌다"는 피드백으로 "한도 88.7%" 부분 삭제, 핵심 비교(390MB → 64MB)만 남김
- "무료 티어를 정말 초과한 게 맞는지"에 대한 재확인 요청 — Supabase 정확한 과금 한도를 확신할 수 없어, "한도 초과"라는 인과관계 대신 "유료 전환"이라는 확실한 사실관계로 표현 유지하기로 함(원본 스크린샷으로 재확인 필요 항목으로 남음)
- 최종 수치: 390MB → 64MB, 약 84% 감소 (다른 곳과 통일된 형식: 괄호 안에 수치, 퍼센트는 `<b>`)

### 개인 기술 블로그 — Kanto와 같은 시각 언어로 재구성
- "주제 문장 + 하위 상세 리스트(`border-l-0 pl-0`)" 구조로 Kanto와 통일
- Playwright 관련 내용을 프로젝트 전반에서 완전히 제거: 전역 기술 스택의 "Testing" 행(Vitest·Testing Library·Playwright) 자체를 삭제, 블로그 프로젝트의 "Next.js/React 전환 적합성 검토" 불릿과 기술 스택 태그에서도 제거
- 이에 따라 그 불릿이 근거였던 "2026.07~ 진행중" 날짜 표기도 원래대로("2026.01 - 2026.03") 되돌아감
- **배럴 파일(Barrel File) 패턴 발견 및 반영** — 사용자가 "babel?"로 기억하던 것을 실제 커밋(2026-03-06, `b9eaffe`)에서 확인. `components/about/index.js`가 하위 모듈을 모아 재노출하는 배럴 파일이었고, 바로 다음 커밋(`b3a576c`)에서 길어진 `initDesktop` 함수를 `initHeader`/`initAside`/`initAppIcons`로 쪼갠 것도 확인. 새 불릿으로 추가:
  ```
  기능 단위마다 배럴 파일(index.js/section.js)로 하위 모듈을 재노출하는 구조 설계
    - 단일 함수에 몰린 로직을 initHeader·initAside·initAppIcons 등으로 분리
    - 분리한 함수들을 배럴 파일로 다시 묶어 하나의 진입점으로 제공
  ```
- **"MPA 구조로 로딩 성능 최적화" → "MPA 구조로 설계"** — `myblog.md`가 이 프로젝트의 유일한 수치 기반 성능 작업은 markdown-block-preview뿐이라고 명시한 것에 따라, 측정 안 된 "최적화"라는 단어를 빼고 사실(그렇게 설계했다)만 남김.

### TanStack Query — GET 요청 감소 실측 (18회 → 3회, 83.3%)
`useState`+`useEffect`로 관리하던 "이전" 상태를 하위 항목으로 명시하고, 실측한 GET 요청 감소치(18→3회)를 "서버/UI 상태 분리" 항목과 합쳐 하나로 정리:
```
useState·useEffect로 직접 관리하던 서버 상태에서 문제 발견
서버/UI 상태 분리, 캐시 교체 방식으로 GET 요청 감소 (18회 → 3회로 약 83.3% 감소)
```
측정 시나리오(탭 5회 왕복 + 룰 등록/목록 2회 왕복)는 이력서에는 넣지 않고 이 문서에만 기록 — 면접 등에서 근거로 설명할 때 쓰는 용도.

## 6. 세부 조정 (3차 라운드) — 외부 피드백 반영 + 레이아웃 버그 수정

### ESLint-KR — 근거 재검증과 전면 재구성
- 포트폴리오 피드백에 "공식 저장소의 거절된 이슈 확인 → **React 커뮤니티 번역 생태계 비교** → 시장 공백 진입"이라는 문구가 반복 등장 — 이 중 "거절된 이슈 발견"은 본인이 쓴 개인 블로그 글(fullstackfamily.com)에서 실제 근거 확인됨. 하지만 "React 생태계 비교 분석"은 본인이 직접 "그냥 생각만 했고 글로 남기진 않았다"고 확인 — **근거 없는 부분은 최종적으로 이력서에서 제외**.
- 최종 구조로 정리:
  1. 문제의식·기여 동기(거절된 이슈 발견, 번역기 품질 검증, v8→v9 전환기 혼란 포착)
  2. 구조·재사용성 설계(Layout+Outlet 페이지 분리, CopyableCodeBlock 공통 컴포넌트, 번역이 아닌 재구성)
  3. React/SPA 선택 판단 — 처음엔 "학습 목적으로 선택"이라고 쓰려 했으나, 본인이 "SPA vs MPA를 이미 알고 있었고 상태관리도 의도적으로 썼다"고 정정 → "SPA가 유리한 부분엔 맞았지만, 정적 콘텐츠 탐색까지 클라이언트 라우팅으로 처리해 GitHub Pages 딥링크·SEO 문제가 생겼다"는 **스코프 판단 실수**로 재작성
  4. 자체 AI 코드 리뷰·Fork→PR 오픈소스 구조는 포트폴리오에 없는 내용이라는 이유로 **최종 삭제**

### Frontie — Lighthouse 수치 확인, 문제해결 사례 보강
- 외부 피드백이 "이력서(48→75) vs 포트폴리오(48→92) 수치 불일치"를 지적 — 본인 확인 결과 **현재 이력서 수치(75→92)가 맞는 최신값**으로 확정.
- `frontie.md` §2에서 실제 문제해결 사례(스크롤 버그, 마크다운 SVG 렌더링 문제) 발굴해 추가했다가, 이후 지면 조정 과정에서 다시 간결화(현재는 소개+Lighthouse 개선 2줄).

### PR 섹션 — 외부 피드백 기반 압축 및 위치 이동
- 외부 이력서 피드백: "PR 항목이 소개와 내용 중복(AI 활용) + 추상어 비중 높음 + 4문단은 과함 → 절반으로 압축, 소개에 없는 이야기(AOC 체계화, 오픈소스·블로그 운영)만 남기기"
- 3문단(4문장 안팎) → **1문단 3문장**으로 압축, AI 관련 문단(소개와 중복) 전체 삭제
- 이후 섹션 배치 자체도 조정 — 기존엔 2페이지 맨 끝(프로젝트 다음)에 있었는데, **1페이지 기술 스택 바로 아래**로 이동. 최종 구성: 1페이지(헤더·소개·교육·기술 스택·PR) / 2페이지(프로젝트 전체)

### 레이아웃 — 섹션 간격 실험 후 원복
- 1페이지 섹션(소개/교육/기술 스택) 간격을 `mb-10` → `mb-8` → `mb-6`까지 줄여봤다가 **다시 `mb-10`으로 원복**
- 프로젝트 간 간격은 `mb-5` → `mb-8` → **`mb-6`**로 최종 조정

### 프로젝트 섹션 — 시각적 구분 시도 후 원복
- "읽기 어렵다"는 피드백에 따라 각 항목의 주제 문장을 볼드 처리 → "별로"라는 반응으로 철회
- 대신 텍스트 크기(`text-[11pt]`)로 구분 시도 → 이것도 철회, **최종적으로 시각적 강조 없이 원래 상태 유지**(기존 수치 강조용 `<b>`만 남음)

### 페이지 번호("1/2", "2/2") 위치 불일치 — 버그 발견 및 수정
- 화면에서 두 페이지 번호 위치가 달라 보인다는 지적 → 실측 결과, **화면에서는 완전히 일치**(양쪽 다 정확히 297mm)하지만, **인쇄(PDF) 시에는 불일치**했음.
- 원인: `main`에 걸려있던 `print:min-h-0`이 인쇄 시 최소 높이 제한을 풀어버려서, 각 페이지 콘텐츠 실제 높이(1페이지 281.7mm, 2페이지 289.0mm)만큼만 박스가 생성됨. 페이지 번호는 "박스 바닥에서 10mm"로 고정돼 있어서, 물리적 A4 용지(297mm)는 같아도 박스 높이가 다르면 인쇄 시 번호가 서로 다른 높이에 찍히는 구조였음.
- **조치**: `print:min-h-0` 제거. 재측정 결과 인쇄 모드에서도 두 페이지 모두 정확히 297mm로 통일되어 번호 위치 일치 확인.
- **교훈**: 화면 렌더링과 인쇄(PDF) 렌더링은 CSS 규칙(`print:` 유틸리티)에 따라 다르게 동작할 수 있어, 화면만 보고 "괜찮겠지"라고 넘기면 실제 인쇄물에서만 드러나는 버그를 놓칠 수 있음 — 두 가지를 각각 실측해야 확실함.

---

## 미해결 / 확인 필요 항목

- **Kanto "고객지원" 페이지**: `/terms` 라우트의 헤더가 실제로 "고객지원"이라는 별도 브랜드로 디자인돼 있고(`TermsHeader.tsx`, 번역 파일에 `Footer.support.report: "문의/신고하기"`까지 준비돼 있음) 확인됨. 근거는 확보했지만 **아직 이력서에 반영 안 됨.**
- **Supabase 무료 티어 "한도 초과"의 정확한 근거**: 실제 한도 수치를 스크린샷(`kanto/supabaseUsage.png`)으로 재확인 필요.

---

## 2차 개선안 (예정 — 아직 이력서에 반영 안 함)

- **개인 기술 블로그 — Next.js/NestJS(v2.0.0) 마이그레이션**: `myblog.md` 5.2절 기준으로 현재는 "설계 문서만 존재, 미착수" 상태. 지금 이력서엔 "React 전환 **적합성 검토**"(Playwright 실측)까지만 사실대로 반영돼 있고, 실제 마이그레이션 자체는 아직 없음.
  - 실제로 Next.js 16 App Router + NestJS API 서버 + Prisma로 전환이 완료되면, 그때 구현 결과(아키텍처 전환기, 성능/구조 비교, 마이그레이션 과정에서의 트러블슈팅 등)를 새 불릿으로 추가
  - 지금 단계에서 "마이그레이션했다"처럼 앞서서 쓰지 않기 — 검증 안 된 진행 상황을 이력서에 올리지 않는다는 원칙 유지

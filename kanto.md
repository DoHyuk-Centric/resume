# Kanto 프로젝트 정리 (김도혁 기여 기준)

> 저장소: [FRONTENDBOOTCAMP-17th/kanto](https://github.com/FRONTENDBOOTCAMP-17th/kanto) · 배포: [kanto-iota.vercel.app](https://kanto-iota.vercel.app)
> 기간: 2026.05.28 ~ 2026.07.07 (약 6주) · 4인 팀 프로젝트(박소유·김도혁·이동근·임태형) · Next.js 16(App Router) + Supabase(BaaS) + Redis(Upstash) + Xendit
> 부트캠프 파이널 프로젝트, 참가 5개 팀 중 1위

이 문서는 팀 저장소의 커밋 히스토리, README의 "조원별 기여" 섹션, `docs/`·`review/` 폴더의 작업기록·코드리뷰 문서를 근거로 **김도혁이 실제로 수행한 작업만** 정리한 기록입니다. 팀 전체 작업 중 다른 팀원이 담당·완성한 부분은 "함께한 팀원" 맥락으로만 언급하고, 실제 구현 내용으로는 포함하지 않았습니다.

---

## 1. 구현 기능 정리

### 1.1 핵심 기능

팀 README의 기여도 산정에서 4인 중 최고점(44점 = 규모23 + 난이도21, 나머지 박소유31·이동근35·임태형33)을 받은 영역입니다.

| 기능 | 설명 |
|---|---|
| **실시간 1:1 채팅 시스템 (풀 구현)** | 채팅 타입 정의·Zustand 스토어 설계부터 `@supabase/ssr` 도입, Supabase Realtime 기반 메시지 송수신·읽음 처리·도배 방지·페이지네이션·날짜 구분선·낙관적 업데이트·플로팅 위젯까지 채팅 기능 전체를 초석부터 구현. 이후 이동근(미읽음 카운트, 연속 메시지 표시)·임태형(신고·차단)이 이 위에서 기능을 이어받아 확장. |
| **Redis(Upstash) 기반 로그인 보안** | 동일 IP에서 로그인 5회 이상 실패 시 5분간 로그인을 차단하는 서버리스 rate-limit 구현. 이후 다른 기기 로그인 시 기존 세션을 즉시 로그아웃 처리하고 토스트로 알리는 기능, 회원 탈퇴 API(Supabase Admin 클라이언트)까지 인증 보안 전반을 담당. |
| **공통 디자인 시스템 구축** | 박소유가 만든 헤더·공통 컴포넌트 초석 위에서, 하루(`06-12`) 동안 `page-container`·`page-title`/`page-title-lg`·`section-header`·`EmptyState`·teal 버튼(`shadcn variant=teal` + `.btn-primary`)·`TermsContent`를 각각 별도 커밋으로 추출해 CSS 클래스·공통 컴포넌트 체계를 처음 통일. 다음 날(`06-13`) `ContentCard`를 만들어 `MainCard`·`RentalCard`·`UsedGoodsCard` 등 카드 컴포넌트를 한 커밋으로 통합하고, WCAG 2.1 AA 접근성 전면 개선(PR #169 — 아이콘 버튼 18개 파일 `aria-label`, 탭 시맨틱 마크업, `<dl>` 구조화, native 체크박스 전환 등)을 진행. 현재 저장소 기준 `ContentCard` 17개·`page-container` 19개 파일에서 사용 중. |
| **관리자 대시보드 · 운영관리(Operation) 페이지 전체** | `(admin)/layout.tsx` 접근 가드부터 대시보드 KPI·인기 게시글 Top5, 글 관리(소프트 딜리트), 신고 내역 상세(유저·원본 글 연결), 감사 로그, `super_admin` 권한 체계, 운영관리 하위 메뉴(공지·금칙어/스팸 콘텐츠 관리·인기 공고 설정·권한 관리·모니터링)까지 어드민 영역 대부분을 설계·구현. Supabase RPC/인덱스 설계 등 DB 레벨 작업도 포함. |

### 1.2 기능 영역별 상세 (커밋 기준)

**채팅**
- 채팅 목록/채팅방 UI, 실시간 메시지·읽음 처리·도배 방지 훅 분리 (`06-05`~`06-07`)
- 플로팅 채팅 위젯 구현 및 기존 채팅 라우트 제거, 채팅방 나가기·실시간 방 나가기 기능 (`06-15`~`06-21`)
- 예약중 상태 시스템 메시지 자동 추가 (`06-22`)

**인증/로그인**
- 로그인 폼 개선 및 서버사이드 인증 API 추가 (`06-14`)
- Redis+Upstash IP 기반 로그인 실패 제한 (`06-14`)
- 다른 기기 로그인 시 기존 세션 즉시 로그아웃 + 토스트 알림 (`07-04`)

**공통 UI/디자인시스템**
- 스크롤 위치 기반 플로팅 버튼, URL 파라미터 공통 유틸리티, `MainCard`/각 페이지 카드 컴포넌트 제작 (`06-04`~`06-11`)
- 하트 `LikeButton`·`toggleLike` 등 공통 함수 추출 (`06-10`~`06-11`)
- CSS 클래스 체계 구축 — `page-container`/`page-title`/`page-title-lg`/`section-header`/`EmptyState`/teal 버튼(`btn-primary`)/`TermsContent`를 하루 동안 순차 추출 (`06-12`)
- `ContentCard` 공통 컴포넌트 제작 및 `MainCard`·`RentalCard`·`UsedGoodsCard`·`Popular`·`UsedGoodsList` 통합, `usedgoods` CSS 패턴 통합 (`06-13`)
- WCAG 2.1 AA 기준 접근성 전면 개선(PR #169) — 아이콘 버튼 18개 파일 `aria-label`, `aria-expanded`/`aria-haspopup`/`aria-pressed`, 탭 UI `role="tab/tablist/tabpanel"`, `<time dateTime>`, `<dl>/<dt>/<dd>` 구조화, `div onClick`→`<Link>`/`<button>`, 커스텀 체크박스→native `input`+`label` 전환, 접근성 개선 가이드 문서 작성 (`06-13`)

**프로필**
- `ProfileCard`·`ProfileField` 공통 컴포넌트, 프로필 서버 컴포넌트 전환 (`06-04`)
- 활동 통계·거래내역 페이지네이션, 개설/참여 모임 목록, 프로필 사진 즉시 저장 UX (`06-30`)

**이용약관/CMS**
- Notion CMS API 연동 및 인메모리 캐시, 약관 레이아웃·탭 네비게이션 초기 설계 (`06-09`)
- 약관 페이지 자동화 — 매번 수동 작성하던 내용을 Notion에서 제목 기준으로 즉시 갱신되도록 개선 (`06-15`)

**메인페이지 / 검색·필터**
- 히어로 섹션, 인기 목록, 메인 검색바 전체 구현, `MainCard` 재사용 컴포넌트 (`06-05`)
- 중고거래·구인구직·부동산 목록 페이지에 URL 파라미터 기반 검색/필터 공통 적용 (`06-06`~`06-08`)

**관리자(Admin) / 운영관리(Operation)**
- `(admin)/layout.tsx` 접근 가드 추가, `admin` 글 관리 페이지 신규 구현 (`06-18`~`06-19`)
- 관리자 대시보드 인기 게시글 Top5 카드에 상세 페이지 링크 추가, 신고 내역 상세에서 유저 프로필·원본 게시글 링크 연결 (`06-19`)
- 회원 탈퇴 API Route 및 Supabase Admin 클라이언트 추가 (`06-14`)
- 감사 로그 페이지 구현 및 `super_admin` 권한 체계 정비, 권한 관리 페이지 구현 (`06-25`)
- 운영관리 페이지 폴더 구조 설계 — 콘텐츠 관리를 금칙어 관리/스팸 관리로 분리, 공지 관리 Supabase API 구현, 공지 점검 페이지 구현 (`06-25`)
- 도배글 방지 정책을 관리자 운영 정책 화면에서 직접 관리하도록 구현 — 기본값 "1분에 3번 글 작성" 초과 시 작성 차단 (`06-30`), 구인구직 인기 공고를 관리자가 수동 지정할 수 있는 기능 (`06-30`)
- 권한 관리 컴포넌트 분리, 관리자 통계 모니터링 탭 세션별 컴포넌트 분리, 콘텐츠 관리 리팩토링, 관리자 로고 작업, 모바일 반응형 개선 (`06-26`)
- 관리자 페이지 반응형 개선 — 테이블 UI를 카드형으로 전환, 페이지네이션 버튼 최적화 (`06-28`)
- 어드민 콘텐츠 영역 리팩터링 — TanStack Query 도입으로 서버/UI 상태 분리, 캐시 교체 방식으로 불필요한 재요청 차단 (`06-26`)

**KTS·KPPS(망고 지수) 신뢰도/인기 점수**
- KTS·KPPS 점수 시스템 DB 마이그레이션 및 타입 추가, 프로필 페이지 망고 지수 UI, KPPS 기반 인기 뱃지 구현 (`06-20`~`06-23`)

**SEO/성능/국제화**
- SEO 메타데이터·OG 이미지·JSON-LD 구조화 데이터 (`06-27`~`07-04`)
- 이미지 WebP 변환으로 Supabase Storage 트래픽 최적화 (`07-03` 전후)
- 공지사항 MyMemory 번역 API 연동 — 등록 시 한/영/필 자동 번역·저장 (`06-29`)

**AI 챗봇**
- 멀티 LLM(Groq·Gemini·Cerebras) 폴백 기반 챗봇 UI 및 API 라우터 구현 (`06-23`~`07-02`, PR #387/#395)

**비즈니스 페이지 — 철회**
- 업종 소분류 데이터와 비즈니스 페이지 초안 작업을 진행했으나(PR #379), 완성도·우선순위 판단 후 본인이 직접 되돌림. WBS/PR에 명시된 자체 판단 하에 스코프를 축소한 사례.

---

## 2. 문제 해결 과정

### 2.1 로그인 보안 — Redis(Upstash) 기반 무차별 대입 방어

- **문제 정의**: 로그인 API에 시도 횟수 제한이 없어 무차별 대입(brute-force) 공격에 취약했다.
- **원인**: 서버리스(Vercel) 환경에서는 인스턴스 간 상태 공유가 안 되어 in-memory 카운터로는 IP별 시도 횟수를 추적할 수 없었다.
- **해결책 설계 및 선택**: Upstash Redis를 도입해 IP를 키로 실패 횟수를 카운트하고, 5회 이상 실패 시 5분간 로그인 자체를 차단하도록 설계. 서버리스 환경에서 연결 유지 비용 없이 동작하는 Upstash의 REST 기반 클라이언트를 선택.
- **구현 및 검증**: `redis 연결 - redis와 upstash를 사용하여 사용자의 IP를 조회해 5회이상 로그인 실패시에 IP를 5분간 로그인 불가능 상태로 제한` 커밋(`06-14`)으로 구현. 이후 다른 기기 로그인 시 기존 세션을 즉시 로그아웃시키는 기능(`07-04`)까지 이어서 인증 보안을 보강.

### 2.2 삭제된 게시글과 거래내역이 함께 깨지는 문제

- **문제 정의**: 거래가 진행 중인 게시글을 삭제하면 FK 제약으로 삭제 자체가 막히거나, 삭제 후에는 거래내역 화면에서 게시글 제목을 표시할 방법이 없었다.
- **원인**: `transactions.post_id`가 `posts.id`를 참조하는 일반 FK로 걸려 있어 참조 무결성이 삭제를 막거나, 삭제를 강행하면 제목 정보 자체가 사라지는 구조였다.
- **해결책 설계 및 선택**: `transactions`에 `post_title` 스냅샷 컬럼을 추가해 거래 시점의 제목을 별도로 보존하고, `post_id` FK를 `SET NULL`로 변경해 게시글이 삭제돼도 거래 기록 자체는 끊기지 않도록 설계.
- **구현 및 검증**: `fix: 삭제된 게시글의 거래 내역 표시가 깨지는 문제 수정`(`07-04`) 커밋으로 반영. 이후 `삭제된 테이블 참조 함수 및 중복 트리거 정리`, `미사용 DB 테이블 정리 및 스키마 문서 추가`(`07-06`)로 관련 스키마를 함께 정리.

### 2.3 이미지 업로드로 인한 Supabase Storage 트래픽 최적화

- **문제 정의**: 이미지를 원본 그대로 업로드·렌더링하면서 Supabase Storage 트래픽이 과도하게 발생.
- **원인**: 업로드 단계에서 별도의 압축·포맷 변환이 없어 수 MB 단위 원본 이미지가 그대로 저장·전송되고 있었다.
- **해결책 설계 및 선택**: 업로드 전 클라이언트에서 WebP로 변환해 전송량 자체를 줄이는 방향을 담당(팀 내 이동근이 AVIF 포맷·캐시 TTL·LCP priority를 이어서 보강).
- **구현 및 검증**: 렌탈 이미지 업로드 스크립트에 WebP 변환을 적용(`07-01` 전후)했고, README 트러블슈팅 기록상 최종적으로 Storage 트래픽이 대폭 감소한 것으로 확인됨.

### 2.4 성능 개선 배포 후 회귀 조사

- **문제 정의**: `docs/성능` 디렉터리의 `psi-check` 스크립트로 배포마다 Core Web Vitals(LCP·INP·CLS)를 자동 측정하는 체계를 운용하던 중, 배포 후 재측정에서 일부 지표가 기대만큼 개선되지 않거나 회귀하는 구간이 발견됨.
- **원인**: 배포 환경과 로컬 측정 환경의 차이, 캐시 워밍 여부 등으로 단발성 측정만으로는 실제 개선 여부를 신뢰하기 어려웠다.
- **해결책 설계 및 선택**: 배포 후 재측정을 별도 문서화 루틴으로 만들어, 회귀가 발견되면 원인을 조사해 기록으로 남기는 방식을 도입.
- **구현 및 검증**: `docs: 성능 개선 배포 후 재측정 결과 반영 및 회귀 원인 조사`(`07-06`) 문서화 작업으로 정리. 측정 자동화(`psi-check`)와 재측정 원칙을 결합해 "감으로 판단하지 않는" 성능 검증 루틴을 구축.

### 2.5 관리자 대시보드 — Supabase JS 체이닝으로 표현 안 되는 집계 쿼리

- **문제 정의**: 어드민 대시보드가 필요로 하는 통계 중 일부는 여러 테이블을 합쳐야 나온다. 예를 들어 지역별 게시글 수는 `used_goods`·`rentals`·`jobs` 세 테이블에 지역 정보가 나뉘어 있고, 활성 사용자 수는 `posts`·`chats`·`comments`를 모두 합산해야 했다.
- **원인**: Supabase JS 클라이언트의 체이닝 API로는 이런 다중 테이블 집계를 한 번에 표현할 수 없어, 쿼리를 여러 번 나눠 보내고 프론트에서 다시 합치는 방식이 되어야 했다. 여기에 대시보드는 진입 시 쿼리 14개를 동시에 실행하는데, `common_reports`/`posts`/`users`가 자주 조건으로 쓰는 `status`·`created_at`·`deleted_at` 컬럼에 인덱스가 없어 매번 풀스캔이 발생했고, 신고 처리 완료 시각을 남길 `resolved_at` 컬럼 자체가 없어 처리율·평균 처리시간 통계를 낼 수 없었다.
- **해결책 설계 및 선택**: 집계가 필요한 부분은 Postgres 함수(RPC)로 등록해 DB 안에서 한 번에 처리하고 결과만 받아오는 방식을 선택. 별도로 인덱스 3개와 `resolved_at` 컬럼을 추가하는 마이그레이션 SQL을 분리 작성.
- **구현 및 검증**: `docs/설계/admin-dashboard-supabase-spec.md`에 문제 정의·SQL을 문서화하고, `admin_setup_01_indexes.sql`(인덱스 3개 + `resolved_at` 컬럼)·`admin_setup_02_rpc.sql`(RPC 함수 5개)을 `CREATE INDEX IF NOT EXISTS`/`ADD COLUMN IF NOT EXISTS`/`CREATE OR REPLACE FUNCTION` 형태로 작성해 중복 실행해도 안전하도록 구현. 감사 로그·`super_admin` 권한 체계 정비(`06-25`)로 이어짐.

---

## 3. 성능 개선 관련 내용

- **Core Web Vitals 자동 측정**: `docs/성능/scripts/psi-check.mjs`를 활용한 배포마다의 지표 자동 측정 체계에 참여. LCP 이미지 `priority` 속성 적용, WebP 포맷 전환을 담당 영역으로 진행.
- **이미지 트래픽 최적화**: 업로드 단계 WebP 변환으로 Storage 트래픽을 줄이는 방향을 주도(§2.3). 캐시 TTL·AVIF 포맷 등 나머지 개선은 이동근이 이어받아 완성.
- **어드민 콘텐츠 영역 성능 리팩터링**: TanStack Query 도입으로 탭 이동 시 30초 이내 불필요한 재요청을 차단하고, 템플릿 저장 시 전체 목록 재요청 대신 캐시만 교체하도록 개선(`06-26`) — GET 요청 수를 줄이는 방향의 최적화.
- **프로젝트 완성도 자가 진단**: 개발 중반(`2026-06-26`) AI(Claude)를 활용해 로컬 저장소 전체를 분석하는 `PROJECT_ASSESSMENT.md`를 직접 작성. `/business` 하위 라우트 3개가 `page.tsx` 누락으로 404가 발생하는 등 미완성 지점을 코드 레벨에서 찾아내 팀에 공유 — 이후 해당 페이지를 철회 처리하는 판단(§1.2)으로 이어짐.

---

## 4. 아키텍처 구조 (담당 영역 중심)

### 4.1 전체 구조에서 김도혁이 설계·구현한 영역

```
[브라우저]
   │
   ├─ 채팅 위젯(Zustand Store) ──postgres_changes──▶ [Supabase Realtime] ── 1:1 채팅방
   │                                                        │
   ├─ 로그인 폼 ──▶ [Server Action] ──▶ [Upstash Redis]  IP 실패 카운트 · 5회/5분 차단
   │                                        │
   │                                        └─▶ [Supabase Auth] 세션 발급 · 타 기기 로그인 시 기존 세션 종료
   │
   ├─ 이용약관 페이지 ──▶ [Notion API] (인메모리 캐시) ── 제목 기준 CMS 콘텐츠 자동 갱신
   │
   └─ 공통 UI(ContentCard/EmptyState/CSS 클래스 체계) ── 전 페이지 공통 사용
```

### 4.2 채팅 시스템 데이터 흐름 (직접 설계)

```
채팅 시작 → useChatRoomRealtime (Realtime 구독)
  → 메시지 전송 → 낙관적 업데이트(tempId 기준 임시 반영) → Supabase insert
  → 상대방 클라이언트 postgres_changes 이벤트 수신 → 메시지 목록 갱신 + 읽음 처리
  → 도배 방지 훅(useSpamPrevention) → 일정 시간 내 과다 전송 시 차단
  → 채팅방 이탈 시 Realtime 구독 해제(cleanup)
```

- 1차 코드 리뷰(§6)에서 지적된 `useChatMessages` 의존성 배열 누락, `useSpamPrevention`의 `setInterval` cleanup 누락은 본인이 직접 수정.

### 4.3 인증 보안 흐름

```
로그인 시도 → Upstash Redis에 IP 키로 실패 횟수 기록
  → 5회 이상 실패 시 5분간 로그인 API 자체를 차단
  → 로그인 성공 시 Supabase Auth 세션 발급
  → 다른 기기에서 재로그인 시 기존 세션을 서버에서 즉시 무효화 + 클라이언트 토스트 알림
```

### 4.4 관리자(Admin) / 운영관리(Operation) 구조

```
(admin)/layout.tsx  ── 접근 가드 (super_admin 권한 체계)
   │
   ├─ 대시보드 ── RPC(admin_setup_02_rpc.sql) 5개 함수로 다중 테이블 집계(지역별 게시글 수·활성 사용자 수 등) 1회 조회
   │              KPI 카드 · 인기 게시글 Top5(상세 링크) · 신고 현황 요약
   ├─ 글 관리 ── 소프트 딜리트, 상세 드로어
   ├─ 신고 내역 ── 상세에서 유저 프로필·원본 게시글 링크 연결
   ├─ 감사 로그 ── super_admin 권한 체계와 함께 정비
   └─ 운영관리(Operation)
        ├─ 공지 관리 ── Supabase API + 공지 점검 페이지
        ├─ 콘텐츠 관리 ── 금칙어 관리 / 스팸 관리로 폴더 분리
        ├─ 인기 공고 설정 ── 메인 노출 구인 공고 수동 지정
        ├─ 권한 관리 ── 컴포넌트 분리
        └─ 모니터링 ── 통계·성능·에러 탭, 세션별 컴포넌트 분리
```

- 대시보드 진입 시 발생하던 14개 동시 쿼리의 인덱스 부재·집계 불가 문제를 RPC + 인덱스 마이그레이션으로 해결(§2.5).
- 어드민 콘텐츠 영역은 이후 TanStack Query로 리팩터링해 탭 이동 시 불필요한 재요청을 차단(§3).

### 4.5 DB 스키마 정리 작업

- `posts`/`transactions` 등 운영 테이블에서 삭제된 테이블을 참조하던 함수, 중복 트리거를 찾아 정리하고 스키마 문서를 추가(`07-06`).
- Supabase `pg_cron` 스케줄을 통합 정리하는 문서화 작업 수행 — 여러 스케줄러가 산발적으로 등록되어 있던 것을 한 곳에서 파악 가능하도록 정리.

---

## 5. 기타 — 미해결 사항 / 향후 계획

- **AI 도입 설계**: WBS 기준 `AI 도입 설계`(2026-06-23~06-30)가 "진행 중" 상태로 기록되어 있음 — 멀티 LLM 챗봇(Groq·Gemini·Cerebras)은 구현했지만, 설계 문서 자체는 계속 보강 중이었던 것으로 확인됨.
- **비즈니스 페이지**: 본인이 시작했던 기능(PR #379)을 스스로 철회 처리한 상태로 남아 있음 — 재작업 여부는 프로젝트 종료 시점 기준 미정.
- **회고**: 부트캠프 파이널 프로젝트로 참가 5개 팀 중 1위를 기록. 실시간 채팅과 인증 보안, 디자인 시스템까지 폭넓은 영역을 초석부터 담당하면서 팀 내 기술적 트레이드오프를 문서로 정리해 결정하는 과정을 경험했고, AI를 활용해 코드 리뷰 대응과 프로젝트 진단, 보고서 작성을 자동화하며 팀의 개발 효율을 높이는 역할도 함께 수행함(§6).

---

## 6. 협업

팀 프로젝트이기 때문에, 이 섹션은 김도혁이 협업 과정에서 실제로 맡았던 역할과 팀 전체의 협업 체계 중 본인이 직접 관여한 부분을 정리했다.

### 6.1 팀 협업 체계 (README 기준)

- **소통 도구**: Discord(실시간 소통·화면 공유 페어 디버깅), Notion(WBS·기획·회의록·트러블슈팅 기록), Figma(UI 시안 공유)
- **회의**: 하루 한 번 정기 스탠드업으로 진행 상황 공유, 불참자를 위해 회의록을 Notion에 기록
- **역할 분담**: 기능 단위로 한 사람이 처음부터 끝까지 책임지되, 다른 팀원이 이어받을 수 있도록 설계 의도를 문서화하는 방식(4인 합산 커밋 1,191개)
- **PR 정책**: 본인 외 1인 이상 Approve 필수, PR 단위는 기능 단위 유지
- **브랜치 전략**: `main`(배포)/`release`(QA)/`develop`(통합)/`feature/*`/`fix/*`/`hotfix/*`, 커밋 컨벤션은 `<type>: <설명>`(`feat`/`fix`/`refactor`/`chore`/`bug`/`hotfix`/`docs`)

### 6.2 본인이 직접 작성한 협업 규칙·문서

- **핫픽스 브랜치 룰 초안 작성** — `docs/hotfix-rules.md`. `hotfix-kanto-{이슈번호}` 네이밍, "해당 이슈의 버그 수정만 커밋(기능 추가·리팩토링 금지)", "develop에서 분기 후 develop으로 PR", "리뷰어 최소 1인 승인" 등 배포 후 긴급 대응 규칙을 팀 전체에 적용되도록 직접 문서화(`07-01`).
- **코드 리뷰 개선 태스크 할당 문서 작성** — `docs/작업기록/20260611-리뷰_개선_할당.md`. 외부 멘토(`urstory`)가 남긴 1~4차 코드 리뷰 내용을 팀원별 담당 파일·우선순위(🔴 필수/🟡 제안)·완료 여부가 있는 표로 재구성해 팀 전체가 진행 상황을 추적할 수 있도록 정리. 본인 몫(`useChatMessages` 의존성 배열, `useSpamPrevention` cleanup, Notion API fallback 등)도 이 문서 기준으로 전부 완료 처리.
- **리뷰 할당 문서 우선순위 요약 갱신**(`06-11`) — 회차가 늘어나며 늘어난 리뷰 항목들의 우선순위를 다시 정리해 팀이 무엇부터 처리해야 하는지 명확히 함.

### 6.3 외부 멘토 코드 리뷰 대응

- 저장소에는 외부 멘토 계정(`urstory`)이 6주간 20회 이상(`202606080939` ~ `202607071004`) 정기적으로 남긴 코드 리뷰·가이드 문서(`review/` 폴더)가 쌓여 있으며, 마지막 주에는 "마감 전 최종 점검", "발표 전 마지막 점검" 문서까지 이어짐.
- 본인은 1~4차 리뷰에서 지적된 항목(읽음 처리 `useEffect` 의존성 누락, 낙관적 메시지 교체 방식, Notion 외부 API 실패 시 fallback 부재, 서버 컴포넌트 전환 등)을 확인·수정하고, 위 6.2의 할당 문서로 팀 전체 리뷰 대응 현황을 추적 가능하게 만드는 역할을 겸함.

### 6.4 팀원과의 협업 지점 (README "협업 기능" 기준)

같은 기능을 이어받거나 병행한 경우를 팀 README가 명시한 그대로 정리하면 다음과 같다.

| 기능 | 함께한 팀원 | 협업 내용 |
|---|---|---|
| 채팅 고도화 | 이동근, 임태형 | 본인이 만든 채팅 초석 위에 이동근이 미읽음 카운트 버그 수정·연속 메시지 표시를, 임태형이 채팅방 신고·차단을 이어 구현 |
| 망고지수(KTS) | 이동근 | 본인이 KTS·KPPS 신뢰 점수 시스템 DB 마이그레이션을, 이동근이 정지 해제 시 망고지수 복원을 담당 |
| 이미지 최적화 | 이동근, 임태형 | 본인이 WebP 변환으로 Storage 트래픽을 최적화, 이동근이 AVIF 포맷·캐시 TTL·LCP priority, 임태형이 업로드 훅·드래그 재정렬을 담당 |
| 공통 디자인 시스템 | 박소유 | 박소유의 헤더·공통 컴포넌트 초석을 본인이 `ContentCard`·CSS 클래스 체계로 확장 |

### 6.5 그 밖의 협업 관련 활동

- **머지 충돌 해결**(`06-23`) — 여러 명이 동시에 작업하던 브랜치를 `develop`에 병합하는 과정에서 발생한 충돌을 직접 해결.
- **네이밍 컨벤션 정리**(`06-10`~`06-11`) — `useJob.ts → job.ts` 등 파일명을 팀 컨벤션에 맞게 리네이밍하고, job 타입을 목적별 파일로 분리해 컨벤션을 통일.
- **파일 책임 분리 원칙 적용**(`06-26`) — 어드민 리팩터링 시 "각각의 파일이 모두 독립성을 갖도록 캡슐화"하는 방향으로 작업해, 다른 팀원이 해당 영역을 이어받기 쉽도록 구조화.
- **AI 활용 협업 자동화** — 코드 리뷰 대응 정리, 프로젝트 완성도 진단(`PROJECT_ASSESSMENT.md`), 보고서 초안 작성 등에 AI를 도구로 활용해 팀의 문서화·의사결정 속도를 높이는 역할을 자발적으로 수행.
